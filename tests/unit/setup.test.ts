import { describe, expect, it } from "vitest";

// Test de humo: confirma que el runner corre y que el alias "@/" resuelve.
// Se borra cuando exista el primer cálculo real (tarea 2.6, somatotipo).
describe("configuración del proyecto", () => {
  it("ejecuta tests", () => {
    expect(true).toBe(true);
  });
});
